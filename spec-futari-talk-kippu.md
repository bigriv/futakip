# ふたりトークきっぷ 実装仕様書

## 1. プロダクト概要

電車移動中に、友達・恋人の2人が **1台のスマホを渡し合って遊ぶ** ブラウザゲーム集。

| 項目 | 内容 |
|---|---|
| 対象 | 2人プレイ専用(パス&プレイ方式) |
| 動作環境 | スマホのモバイルブラウザ(iOS Safari / Android Chrome) |
| 通信 | 外部APIへの通信なし。アセットはすべてビルド時にバンドルする(Webフォント・外部CDN禁止) |
| 成果物 | Nuxt 4(SPA/SSGモード)でビルドした静的サイト |
| データ保存 | 不要(セッション中のJS変数のみ。localStorage等は使わない) |
| 収録ゲーム | ①ベスト&ワースト予想 ②ピッタリ度メーター ③トンネルしりとり |

### 共通の遊びの流れ(パス&プレイ)
「片方だけが画面を見る → 画面を伏せて渡す → もう片方が操作」を画面遷移で誘導する。
**「画面を伏せて渡してね」の中間スクリーンを必ず挟み、受け取った側のタップで次へ進む**こと(誤って相手の秘密情報が見える事故を防ぐ)。

---

## 2. 画面構成

```
ホーム(改札)
 ├─ ① ベスト&ワースト予想
 ├─ ② ピッタリ度メーター
 └─ ③ トンネルしりとり(難易度選択 → ゲーム)
```

- ホームには3つのゲームが「切符」型のカードで並ぶ。タイトル・簡単な説明・ジャンルバッジ(推理/協力/心理戦)付き。
- 各ゲームからは「← 改札へ」ボタンでホームに戻れる(確認ダイアログ不要、ただしプレイ中の進行は破棄されてよい)。

---

## 3. ゲーム① ベスト&ワースト予想(推理)

### コンセプト
お題に対する選択肢の中から、こたえる人がこっそり選んだ「1位」と「最下位」を相手が予想する。

### ルール・フロー
1. **出題**: お題1つ+選択肢6個を表示。「こたえる人だけ見てね」の表示付き。
2. こたえる人が選択肢から **1位** をタップ → 続けて **最下位** をタップ(同じ選択肢は選べない)。
3. **交代スクリーン**(画面を伏せて渡す)。
4. あてる人が同じ選択肢一覧から **1位の予想** と **最下位の予想** をタップ。
5. **結果表示**: 正解の1位・最下位を公開。
   - 片方的中: **1点**
   - 両方的中: **3点**(ボーナス込み)
   - 結果画面に「なんでそれが最下位か聞いてみよう」等の会話を促す一言を添える。
6. 役割を交代して次の問題へ。**全6問**で終了し、合計点に応じた称号コメントを表示(4段階程度)。

### お題データ
- **20問以上** 用意する。形式: `{ theme: "お題文", options: ["選択肢×6"] }`
- 例:
  - 「休日にしがちなこと」→ 寝だめ / カフェ巡り / 掃除 / ゲーム / 散歩 / 動画を見続ける
  - 「もらってうれしい差し入れ」→ からあげ / プリン / エナジードリンク / フルーツ / ポテチ / 肉まん
  - 「デートで行くならうれしい場所」「学生時代に戻ったらやりたいこと」「引っ越し先に欲しい条件」など
- 友達同士でも恋人でも使える内容にする(下ネタ・過度に恋愛前提の質問は避ける)。
- 出題順はシャッフル。1セッション内で同じお題を繰り返さない(尽きたら再シャッフル)。

---

## 4. ゲーム② ピッタリ度メーター(ito風・協力)

### コンセプト
ボードゲーム「ito」の仕組みを参考にした協力ゲーム。こたえる人だけが知る **秘密の数字(1〜100)** を、お題スケール上の「たとえ」を口で言って伝え、あてる人がスライダーで数値を予想。誤差の小ささを2人で競う。

### ルール・フロー
1. **出題**: こたえる人にだけ「お題スケール」と「秘密の数字」を表示。
   - 例: お題「食べ物の人気度」(1=不人気 ⇔ 100=大人気)、秘密の数字 **72**
2. 画面下部にヒント: 「数字は言わずに、その数字っぽい"たとえ"を口で伝えよう(例: おにぎりくらいかな)」
3. こたえる人が口頭でたとえを言ったら、**交代スクリーン**を経て渡す。
4. あてる人は同じお題スケールを見ながら、**1〜100のスライダー**で予想値を決定。
   - スライダーは大きく操作しやすく。現在値を大きな数字で表示。初期値は50。
5. **結果表示**: 秘密の数字と予想値を両方表示し、誤差で判定:
   - 誤差 0〜5: 「以心伝心!」(3点)
   - 誤差 6〜15: 「いいかんじ」(2点)
   - 誤差 16〜30: 「おしい」(1点)
   - 誤差 31以上: 「すれちがい…」(0点)
   - メーター上に「秘密の数字」と「予想」の2点をビジュアル表示すると分かりやすい。
6. 役割交代して **全5ラウンド**。合計点で2人の「通じ合い度」称号を表示(4段階程度)。勝敗ではなく協力結果として見せる。

### 仕様詳細
- 秘密の数字は 1〜100 の一様乱数。ただし **極端すぎる値(1〜5, 96〜100)は出にくくしてもよい**(任意)。
- お題スケールは **30個以上**。形式: `{ theme: "強そうな動物", low: "よわい", high: "つよい" }`
- 例: 強そうな動物 / テンション上がる出来事 / デートでうれしい場所 / 便利な発明 / 朝ごはんに食べたいもの / モテそうな趣味 / 二度と食べたくない組み合わせ / カラオケで盛り上がる曲 など、「たとえ」を出しやすいものを選ぶ。

---

## 5. ゲーム③ トンネルしりとり(記憶・心理戦)

### コンセプト
**使ったワードが相手に見えないしりとり。** 相手に見えるのは「次の頭文字」だけ。すでに出た言葉(自分・相手どちらの分も)をもう一度入力した瞬間に負け。決着後に全履歴が公開されるネタバラシが最大の盛り上がりどころ。

### 難易度(開始時に選択・列車種別モチーフ)

| 種別 | 制限 |
|---|---|
| 各駅停車 | 文字数制限なし |
| 急行 | **3文字ちょうど** の言葉のみ |
| 特急 | **4文字ちょうど** の言葉のみ |
| 新幹線 | **5文字ちょうど** の言葉のみ |

- 文字数カウントは **「ゃ・ゅ・ょ・っ・ー」も1文字として数える**(画面に明記)。
- 難易度選択画面に各制限の説明を表示。

### ルール・フロー
1. 難易度選択 → 最初の頭文字をランダムで提示(「を」「ん」「ー」および小さい文字は除外)。
2. **自分の番**: 画面には「次は『◯』から」+ターン数+文字数制限のみ表示。過去の言葉は一切表示しない。テキスト入力欄に言葉をこっそり入力して決定(声に出さない)。
3. 決定後、**交代スクリーン**(「画面を伏せて渡してね」)。次の人は頭文字だけを見て入力。
4. これを交互に繰り返し、**負け条件**を満たした時点で決着 → **全履歴公開画面**へ。

### 負け条件(この2つだけ)
1. **「ん」で終わる言葉を入力した**
2. **すでに出た言葉と重複した**(自分・相手どちらが使った言葉でも対象)

### バリデーション(負けではなく「入力し直し」になるエラー)
入力決定時に以下をチェックし、エラーメッセージを表示して再入力させる:

1. **文字種**: ひらがな・カタカナ・伸ばし棒「ー」以外(漢字・英数字・記号・空白)はエラー。**カタカナは内部でひらがなに正規化**してから全判定を行う(=「リンゴ」と「りんご」は同一語として重複判定)。
2. **頭文字不一致**: 指定の頭文字で始まっていない場合エラー。
3. **文字数不一致**(急行/特急/新幹線のみ): 指定文字数ちょうどでなければエラー。正規化後の文字列長で判定。
4. **1文字の言葉**はエラー(各駅停車でも最低2文字)。

### 次の頭文字の決定ロジック(正規化後の末尾から)
1. 末尾が「ー」→ その直前の文字を末尾とみなす(「ラッカー」→「か」)。末尾処理後も「ー」なら更に遡る。
2. 末尾が小さい文字(ゃゅょぁぃぅぇぉっ)→ 大きい文字に変換(「じてんしゃ」→「や」)。
3. 末尾の濁点・半濁点は保持しつつ、**次の言葉は「濁点付き」「濁点なし」どちらの頭文字でも可**とする(「ぶ」終わり → 「ぶ」「ふ」どちら始まりもOK)。逆(「ふ」終わりに「ぶ」始まり)も許可する。
4. 末尾が「ん」→ 負け(上記のとおり)。末尾が「を」の言葉は「お」始まりとして扱う。

### 実在語チェックについて
オフラインのため辞書判定は **実装しない**。「実在する言葉かどうかは2人の話し合いで(異議あり方式)」とルール画面に明記する。

### 全履歴公開画面(ネタバラシ)
- 決着理由(「『りんご』はもう出てた!」等)を大きく表示。
- 使われた全ワードを **ターン順・発言者付き** で一覧表示。重複を踏んだワードと元のワードをハイライト。
- ターン数(トンネルを何駅ぶん進んだか)を実績として表示。
- 「もう1本(同じ難易度)」「難易度を変える」「改札へ」のボタン。

### その他
- 制限時間なし。長考は口頭運用に任せる。
- 入力欄は `type="text"`。フォーカス時にソフトキーボードが出るため、入力画面はキーボードで隠れないレイアウトにする(入力欄を画面上部寄りに配置)。

---

## 6. デザイン指針(既存デザインの継続)

レトロな「硬券きっぷ」テーマを継続する。

### カラートークン

`tailwind.config.ts` の `theme.extend.colors` で定義する。

| トークン名 | 値 | 用途 |
|---|---|---|
| navy | #1F2A44 | 背景: 夜行列車の紺 |
| navy-deep | #161F33 | 背景グラデ下端 |
| paper | #F6EEDC | カード: 硬券クリーム |
| paper-edge | #E4D8BE | カードのミシン目・罫線 |
| ink | #2B2A26 | カード上の文字 |
| orange | #EE8635 | メインアクセント(湘南オレンジ) |
| green | #3E7C4F | サブアクセント(湘南グリーン) |
| stamp | #C9452E | 判定スタンプの朱色 |
| muted | #8B93A7 | 補助テキスト |

### ビジュアル要素
- お題や結果は「切符」型カードで表示: クリーム地・左右に丸い切り欠き・上下に破線のミシン目・「乗車券A」「第3問」等の券面ヘッダー。
- 判定演出は **朱色の丸スタンプ**(「的中」「以心伝心」など)が回転しながら押されるアニメーション。`prefers-reduced-motion` 時はアニメーションなしで静的表示。
- フォントはシステムフォントのみ: `"Hiragino Kaku Gothic ProN","Hiragino Sans","Yu Gothic","Noto Sans JP",sans-serif`
- ボタンは指で押しやすい大きさ(高さ48px以上)。`touch-action:manipulation` でダブルタップズーム抑止。`user-select:none`(ただし**しりとりの入力欄は除く**)。
- 最大幅480pxで中央寄せ。縦持ち前提。

### 文言トーン
- ひらがな多め・やわらかい日本語。「〜してね」「おつかれさま」調。
- 鉄道モチーフの遊び(「改札へ」「◯◯区間」「各駅停車」)を全体で統一。

## 7. 実装メモ

- 状態管理は Pinia ストアで行う。画面遷移は Nuxt のページルーティング(`app/pages/`)を使用する。
- 乱数の出題はデッキ方式(シャッフルして pop、尽きたら再シャッフル)で、同一セッション内の重複出題を防ぐ。
- カタカナ→ひらがな正規化: `str.replace(/[\u30A1-\u30F6]/g, c => String.fromCharCode(c.charCodeAt(0) - 0x60))`
- 動作確認観点:
  - しりとり: 「ー」終わり・小さい文字終わり・濁点ゆれ・カタカナ入力・重複(自分の過去語/相手の過去語)・「ん」終わり・文字数制限の各ケース
  - 各ゲームで「伏せて渡す」スクリーンを飛ばして秘密情報が見える経路がないこと
  - iOS Safariでスライダー・テキスト入力が問題なく操作できること
